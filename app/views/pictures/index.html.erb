

<style>
  body { background-color: <%= @color %>; }
</style>
<%= link_to "Add a New Picture", "/pictures/new"%>

<ul>
<% @pictures.each do |picture| %>
  <li>
    <%= link_to image_tag(picture.url, :width=> 100), "/pictures/#{(picture.id)}" %>
  </li>
<% end %>
</ul>

    